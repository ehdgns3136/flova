// import { fromJS } from 'immutable';
// import { makeSelectQuestionDetailPageDomain } from '../selectors';

// const selector = makeSelectQuestionDetailPageDomain();

describe('makeSelectQuestionDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

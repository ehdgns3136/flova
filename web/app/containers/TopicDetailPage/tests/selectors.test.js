// import { fromJS } from 'immutable';
// import { makeSelectTopicDetailPageDomain } from '../selectors';

// const selector = makeSelectTopicDetailPageDomain();

describe('makeSelectTopicDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

// import { fromJS } from 'immutable';
// import { makeSelectIntroPageDomain } from '../selectors';

// const selector = makeSelectIntroPageDomain();

describe('makeSelectInvitePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

import styled from 'styled-components';
import { Message } from 'semantic-ui-react';

export const TransparentMessage = styled(Message) `
  box-shadow: none !important;
  background: none !important;
  color: white !important;
  padding: 0 !important;
`;

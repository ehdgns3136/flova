// import { fromJS } from 'immutable';
// import { makeSelectIntroPasswordResetPageDomain } from '../selectors';

// const selector = makeSelectIntroPasswordResetPageDomain();

describe('makeSelectIntroPasswordResetPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

// import React from 'react';
// import { shallow } from 'enzyme';

// import TermsForm from '../index';

describe('<TermsForm />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

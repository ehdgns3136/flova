// import React from 'react';
// import { shallow } from 'enzyme';

// import TopicForm from '../index';

describe('<TopicForm />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

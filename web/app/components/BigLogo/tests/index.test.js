// import React from 'react';
// import { shallow } from 'enzyme';

// import BigLogo from '../index';

describe('<BigLogo />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

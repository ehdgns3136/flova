// import React from 'react';
// import { shallow } from 'enzyme';

// import IntroPasswordResetForm from '../index';

describe('<IntroPasswordResetForm />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

// import React from 'react';
// import { shallow } from 'enzyme';

// import EmailCheckForm from '../index';

describe('<EmailCheckForm />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});

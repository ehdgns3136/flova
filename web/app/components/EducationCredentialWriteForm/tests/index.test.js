// import React from 'react';
// import { shallow } from 'enzyme';

// import EducationCredentialWriteForm from '../index';

describe('<EducationCredentialWriteForm />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
